# Copy to deploy/.env and fill in real values before `docker compose up`.
# deploy/.env is gitignored — never commit real secrets here.

POSTGRES_USER=grading
POSTGRES_PASSWORD=change-me-to-something-real
POSTGRES_DB=grading

# Must match what deploy/llama-server.service serves.
LLM_MODEL=Qwen/Qwen2.5-Coder-7B-Instruct-GGUF:Q4_K_M
